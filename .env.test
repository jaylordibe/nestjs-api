NODE_ENV="test"
PORT=3001

SERVICE_NAME="nestjs"

DB_USER="${SERVICE_NAME}"
DB_PASSWORD="0UFOZeHfdwbzc2aFzj6dv"
DB_HOST="localhost"
DB_PORT=5433
DB_NAME="${SERVICE_NAME}_test"

DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=public"

REDIS_HOST="localhost"
REDIS_PORT=6378
REDIS_PASSWORD="test-redis-password-not-actually-used"
REDIS_URL="redis://default:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}"

JWT_SECRET="test-only-jwt-secret-not-for-production-use-anywhere-ever"
JWT_EXPIRES_IN="1h"

CORS_ORIGIN="*"

THROTTLE_TTL_MS=60000
THROTTLE_LIMIT=100
